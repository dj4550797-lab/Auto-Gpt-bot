FROM python:3.10-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 10000

# Use gunicorn to run the Flask app for production
CMD ["gunicorn", "-b", "0.0.0.0:10000", "bot:flask_app"]